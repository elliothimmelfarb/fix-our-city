const initialState = {
  tags: ['Broken', 'Potholes', 'Overgrown', 'Dangerous', 'Vandalism', 'Fallen'],
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
